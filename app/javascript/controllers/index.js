// This directory is consumed by the tutorials install generator (Task 14),
// which copies tutorials_controller.js into each host app's
// app/javascript/controllers/ and registers it in the host's Stimulus
// application. The engine itself does not eager-load Stimulus controllers —
// the host owns Stimulus registration via importmap.
